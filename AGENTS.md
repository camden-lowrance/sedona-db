# AGENTS.md

Keep answers concise.

Use Pixi for local SedonaDB development in this fork. Keep fork-local development files such as `AGENTS.md`, `pixi.toml`, `pixi.lock`, and `.gitattributes` on fork-only branches, not on upstream Apache PR branches.

For upstream work, create a clean branch or worktree from `upstream/main` and cherry-pick only the implementation changes needed for the Apache PR.
